instalar as dependencias:
    pip install -r requirements.txt
    
    obs:
        as versoes foram sugeridas pela IA,
        em caso de incompactibilidade mudar versao
        

executar com:
    uvicorn app.main:app --reload